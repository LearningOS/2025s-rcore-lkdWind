# 功能总结
    
​   这节还挺费劲的。刚开始看了很久没发现是银行家算法，不知道如何来抽象这些资源。

   首先就是清楚是要以每个进程为单位来进行资源管理，所以在pcb inner里添加了死锁检测模块。模块数据一维为线程tid，二维为资源rid，模块需要实现几个功能，isunsafe就是银行家算法用来查看当前状态是否安全，tryallocate在检查unsafe同时通过tid和rid处理减小available和need，增加allocation；setneed需要在设置资源前调用，在分配失败时会保留，在分配成功会减小。release在up时增加available和减小allocation；检测开关只需要设置enabled。在创建线程和创建资源时需要更新数据结构，在添加一个线程时会为二维结构添加资源数的全为0的向量，在添加mutex时avail则push 1，sem则push 初始化值。在down时首先判断是否打开了检测，打开了就首先setneed，然后判断unsafe如果不安全则直接返回dead，安全则尝试分配（会失败），然后调用down；up时判断检测打开之后就调用release，release判断了当前分配数是否为0。
    
# 简答作业

1. 线程栈内存（KernelStack 和 UserStack）。
    线程控制块（TaskControlBlock, TCB）包含线程状态、寄存器上下文、优先级等信息。
    文件描述符与打开的文件。
    Mutex、Semaphore 的等待队列需清空，避免死锁。
    动态分配的内存
    TCB引用位置 	是否需要回收	 原因
    调度器的就绪队列	是	    若不回收，可能导致野指针或重复调度已销毁的线程。
    锁的等待队列	    是	    避免唤醒已销毁的线程（如 Mutex::wait_queue 中的 TCB）。
    父线程的子线程列表	是	    主线程需维护子线程列表，退出时需遍历并回收所有子线程的 TCB。
    信号处理上下文	    是	    若线程注册了信号处理函数，需清理相关资源。
    跨线程引用（Arc）	可能	若其他线程通过 Arc<TCB> 持有引用，需确保无强引用时才能回收。
2. 
lock 方法的实现差异
    Mutex1（第 3-12 行）：
    使用 loop 主动重试获取锁，仅在获取锁成功后退出循环。
    如果锁已被占用，将当前线程加入等待队列后立即 block_current_and_run_next()。
    Mutex2（第 27-35 行）：
    无循环，若锁被占用，直接加入等待队列并阻塞。
    线程被唤醒后不会自动重试，依赖 unlock 的唤醒逻辑。
unlock 方法的实现差异
    Mutex1（第 16-23 行）：
    总是释放锁（locked = false），然后唤醒一个等待线程。
    被唤醒的线程需在 lock 中通过循环重新竞争锁。
    Mutex2（第 38-46 行）：
    仅在等待队列为空时才释放锁（locked = false）。
    若有等待线程，直接唤醒它并保持锁的占用状态（锁所有权转移）。
潜在问题
    问题类型	                Mutex1	                    Mutex2
    饥饿（Starvation）	可能：高优先级线程可能重复抢锁。	    无：严格按队列顺序唤醒，公平性更好。
    性能开销	        较高：每次唤醒需重新竞争锁。	        较低：锁所有权直接转移，无竞争。
    锁状态一致性	    可能短暂处于无主状态（locked=false）。	 锁始终有明确所有者（无状态间隙）。

# 荣誉准则

    1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容： 无
        2. 此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容： 无
        3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。
        4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计